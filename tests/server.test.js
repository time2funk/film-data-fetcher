const assert = require('chai').assert;


// describe('Save data to DB', (done) => {
// 	it('Save a record to DB', () => {
// 		var card = new Cards({
// 			name: 'Mario',
// 			weight: 10,
// 			power: 20
// 		});

// 		card.save().then(() => {
// 			assert(card.isNew === false);
// 			done();
// 		});
// 	});
// });